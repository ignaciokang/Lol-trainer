const { TEAM_IDS, TEAM_NAMES } = require("./teams");
const { LANGUAGE_IDS } = require("./languages");
const { PLATFORM_NAMES } = require("./platforms");
const { TIME_IN_MILISECONDS } = require("./timeInMiliseconds");
const REGIONS = require("./regions");

module.exports = {
  TEAM_IDS,
  TEAM_NAMES,
  LANGUAGE_IDS,
  PLATFORM_NAMES,
  TIME_IN_MILISECONDS,
  REGIONS,
};
